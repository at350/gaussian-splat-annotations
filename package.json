{
  "name": "gaussian-splat-annotations",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://at350.github.io/gaussian-splat-annotations/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy" : "gh-pages -d dist -b gh-pages -a -m \"deploy\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "@sparkjsdev/spark": "^0.1.10",
    "three": "^0.180.0"
  }
}
